# Kubuntu 26.04 x86_64 manual evidence — template

Status: pending template; not release evidence or authorization to run lifecycle operations.

Tested commit: `COMMIT_SHA`
Candidate archive SHA-256: `ARCHIVE_SHA256`
Runtime: `PYTHON_AND_GIT_VERSIONS`
Shell: Bash; record optional Zsh coverage separately when available.

## Environment

Record whether the observations came from native Kubuntu 26.04 x86_64, a virtual machine, a container, or emulation. Only the required reviewed native observations complete this target. State which documented flavor criterion was confirmed: explicit operating-system identification, explicit variant identification, or the installed Kubuntu desktop metapackage. An Ubuntu identifier or KDE session alone is insufficient.

Keep hostnames, personal paths, package inventory, raw system metadata, logs, and deployment configuration out of the public record. Record only the generic target, identification conclusion, tested versions, and concise outcomes.

## Installation

After approval of the exact isolated scope and backout, record the public guide steps completed using fresh fictional deployment content. State the resulting candidate installation and managed shell integration behavior.

## Verification

Record host-check recognition, installation replay, verification, and fresh-shell startup observations. Record any unavailable shell or application evidence explicitly. A passing host check alone is not lifecycle acceptance.

## Backout

Record exact-plan Core and shell removal results, shell-profile restoration, and any retained recovery artifacts without publishing private paths or profile content.

## Preservation

Record only the before/after digest conclusion for fresh fictional deployment data and unrelated shell-profile content. State whether repeated integration and removal preserved the approved scope.

## Offline

Record the approved network-isolation method and concise results. Separate any fixture or container rehearsals from native offline observations. Do not infer offline operation from the absence of visible network errors.

## Limitations

Record platform-specific limitations and missing observations. Keep the ledger pending until all required results have been reviewed; this template and automated detection tests do not establish passed evidence.
